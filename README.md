# andrewramey.com

Personal portfolio. Vite + React + TypeScript, deployed to Cloudflare Pages
from `main`.

```bash
npm install
npm run dev       # local development
npm run verify    # typecheck + build — run before pushing
```

See `AGENTS.md` for how the site is built, what it is for, and the compliance
rules that constrain what may be published.
